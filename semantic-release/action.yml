name: Semantic Release
description: Runs Semantic Release

runs:
  using: composite
  steps:
  - name: Setup dependencies
    run: npm --prefix $GITHUB_ACTION_PATH clean-install
    shell: bash
  - name: Copy .releaserc
    run: cp $GITHUB_ACTION_PATH/.releaserc.yaml .
    shell: bash
  - name: Release
    run: npx semantic-release
    env:
      GITHUB_TOKEN: ${{ github.token }}
    shell: bash
